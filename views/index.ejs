<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Create User</title>
    <!-- ✅ Bootstrap CSS (via CDN) -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <%- include('partials/navbar') %>
    <section>
      <div class="container py-5">
        <div class="text-center mb-5">
          <h2 class="fw-bold">🏢 Welcome to Flat Listing Portal</h2>
          <p class="text-muted">
            Here you can find and manage all available flats and users.
          </p>
        </div>

        <!-- Flats Section -->
        <div class="flat-list text-center border p-5 rounded bg-light">
          <h5 class="text-secondary">Flat list will appear here...</h5>
        </div>
      </div>
    </section>
  </body>
  <!-- ✅ Bootstrap JS (via CDN) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</html>
